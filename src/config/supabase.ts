/**
 * Supabase configuration.
 *
 * 👉 PASTE YOUR OWN PROJECT'S VALUES BELOW.
 *
 * You can find them in your Supabase dashboard:
 *   Project Settings → API → Project URL (SUPABASE_URL)
 *                          → Project API keys → anon public (SUPABASE_ANON_KEY)
 *
 * The anon key is safe to expose in the client (it is a public key).
 * Row Level Security policies (see supabase/schema.sql) enforce data isolation.
 */
export const SUPABASE_URL = "PASTE_YOUR_SUPABASE_URL_HERE";
export const SUPABASE_ANON_KEY = "PASTE_YOUR_SUPABASE_ANON_KEY_HERE";
